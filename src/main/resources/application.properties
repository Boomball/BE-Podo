spring.config.import=optional:file:.env[.properties]

spring.application.name=${APP_NAME}

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=${DB_DRIVER}

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO}

# Hibernate SQL ??
#spring.jpa.show-sql=${SHOW_SQL}
#spring.jpa.properties.hibernate.show_sql=${SHOW_SQL_FORMAT}

spring.jpa.properties.hibernate.highlight_sql=${HIBERNATE_HIGHLIGHT_SQL}
logging.level.org.hibernate.SQL=${LOGGING_LEVEL_SQL}
logging.level.org.hibernate.type.descriptor.sql=${LOGGING_LEVEL_SQL_TYPE}
logging.pattern.console=${LOGGING_PATTERN}